@model IEnumerable<Meditatori.Models.Ad>

@{
    ViewData["Title"] = "Index";
}


@foreach (var item in Model)
{
<div class="container">
    <div class="card mb-3" style="max-width: 540px;">
        <div class="row g-0">
            <div class="col-md-4">
                <img src="https://via.placeholder.com/150" class="img-fluid rounded-start" alt="...">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title">@Html.DisplayFor(modelItem => item.Title)</h5>
                    <p class="card-text">@Html.DisplayFor(modelItem => item.Content)</p>
                    <p class="h4">@Html.DisplayFor(modelItem => item.Subject.name)</p>
                    <p class="h4">@Html.DisplayFor(modelItem => item.PricePerSession) Lei</p>
                    <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
                </div>
            </div>
        </div>
    </div>
</div>
}
